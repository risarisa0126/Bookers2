<div class="error">
  <% if book.errors.any? %>
   <%= book.errors.count %>件のerrorが発生しました
     <ul>
     <% book.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
  　  <% end %>
     </ul>
  <% end %>
</div>
<% if flash[:notice] %>
 　<div class="flash">
  <%= flash[:notice] %>
   </div>
  <% end %>

<h2>User info</h2>

 <%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
  <table class="table">
    <tbody>
     <tr>
      <th>name</th>
      <th><%= user.name %></th>
     </tr>
     <tr>
      <th>introduction</t>
      <th><%= user.introduction %></th>
    </tbody>
  </table>
  <%= link_to edit_user_path(user.id), class: 'btn btn-default btn-md btn-block' do %>
  <span class="glyphicon glyphicon-wrench" aria-hidden="true"></span>
  <% end %>

<h2>New book</h2>
  <%= form_for(book) do |f| %>

  <h4>Title</h4>
   <%= f.text_field :title %>
  <h4>Opinion</h4>
   <%= f.text_area :body %>
   <%= f.submit 'Create book',class: "btn btn-primary btn-sm btn-block" %>
   <% end %>
